#!/bin/bash
#
#SBATCH -n 24
#SBATCH -N 1
#SBATCH -t 1-0:0
#SBATCH -J GROUP_CHIR
#SBATCH --export=all

n1=N1;
n2=N2;
rho=RHO;
T=TEMP;
group="GROUP";

cd $SLURM_SUBMIT_DIR
source /home/nam4/.bashrc;

# report
echo "Running on $(hostname)";
echo "Time: $(date)";
echo "Starting directory: $PWD";

conda activate py37;
python /home/nam4/rigid_np/make.py /home/nam4/rigid_np/$group/$group.L.xyz $n1 $n2 $rho $T 24;
/home/nam4/lammps/build/lmp < run.lmp;

echo "Finished on $(date)";
